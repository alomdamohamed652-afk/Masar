export function TopBar(){return <div className="topbar">شحن مجاني للطلبات فوق ١٥٠٠ ج.م <span>·</span> هدية مميزة من مسار مع طلبك 🎁</div>}
function Header({count,user,role}:{count:number,user:any,role:string|null}){const [open,setOpen]=useState(false);const [search,setSearch]=useState("");const nav=useNavigate();const submit=(e:any)=>{e.preventDefault();if(search.trim())nav("/collections?search="+encodeURIComponent(search.trim()))};return <header className="header"><Link to="/" className="logo">مسار<span>MASAR</span></Link><nav className={open?"nav open":"nav"}><NavLink to="/">الرئيسية</NavLink><NavLink to="/collections">المجموعات</NavLink><NavLink to="/faq">الأسئلة الشائعة</NavLink><NavLink to="/contact">اتصل بنا</NavLink>{(role==="admin"||role==="owner")&&<NavLink to="/admin">لوحة التحكم</NavLink>}</nav><form className="header-search" onSubmit={submit}><Search size={17}/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="ابحث عن منتج..." aria-label="البحث عن منتج"/></form><div className="actions"><Link to="/account" aria-label="الحساب"><User size={19}/></Link><Link to="/cart" className="cart-icon"><ShoppingBag size={20}/>{count>0&&<b>{count}</b>}</Link><button className="mobile-menu" onClick={()=>setOpen(!open)}>{open?<X/>:<Menu/>}</button></div></header>}
